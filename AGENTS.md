# AGENTS.md

## 原则

### 1. 简单

- 简单，直到不能更简单。
- 不要考虑可能会遇到或尚未发生的问题。
- 除非：
  - 用户要求。
  - 不处理将直接导致严重的问题，例如正确性、安全性或数据损坏问题。
  - 简单的实现已经被证实有误，或开始造成明显的可维护性问题。
  - 涉及难以逆转或影响广泛的关键架构决策，需要考虑必要的长期影响。
  - 不要考虑兼容性，如果一个破坏性变更能够带来好处或使架构更优雅，执行它。

### 2. 精确

- 主动确认对用户目标和关键约束的理解；对会实质影响结果的不明确因素及时与用户确认。对事实、原因和技术方案保持独立、客观的判断，不盲信用户的预设或结论。
- 对项目现状的判断以代码为准；设计前先了解现有实现和相关先例。
- 文档用于快速了解和定位项目，不能替代代码验证。文档与代码冲突时，以代码为准，并同步修正文档。
- 调研应按需进行：
  - 优先调研项目现有代码。
  - 当现有实现不足以回答设计问题时，可以调研 `codex-cli`、`claude-code`、`gork-build` 等现有 coding agent。
  - 这些项目应位于 `ref/` 下；如果没有，可以 clone 一份。
  - `ref/` 仅用于了解已有方案、内部机制和设计权衡，不作为本项目的设计规范。

### 3. 沉淀

- 当功能、调研或迭代产生值得长期保留的项目知识时，应及时落盘文档。
- 临时排错、中间方案、Agent 思考过程、一次性检索结果以及没有长期价值的小修改，不需要落盘。

文档统一放在 `docs/` 下，按以下类型组织：

- `arch`
  - 关于项目当前架构、机制和设计的说明。
  - 描述“项目现在如何工作”。

- `iter`
  - 历史需求迭代留档。
  - 对于一个完整的需求变更，可以在下面新建文件夹，保存值得长期保留的关键文档，例如：
    - 原始需求。
    - 最终设计方案。
  - 不要保存中间过程文档，例如尚未开始执行时生成的临时计划或草案。

- `research`
  - 存放对外部项目、技术或机制的调研结果。
  - 对 `codex-cli`、`claude-code`、`gork-build` 等项目进行成体系的机制调研后，应维护这里的文档，重点解释其内部实现、关键机制和设计权衡。
  - 零散检索无需单独落盘，除非形成了可复用的结论。

- `todo`
  - 存放短期或长期的项目级远景需求。
  - `todo` 描述的是未来可能需要完成的项目需求，而不是某次任务的执行 plan。

- `docs/` 下各级目录根部都应包含一个 `README.md`，作为该目录下子目录和文档的索引。
- 每条索引使用以下格式：

```text
[标题](相对地址)
description：对于文档内容的简短描述，不多于 150 字符
```

- 在开始调研或了解项目时，先按需检索已有文档，再通过代码验证现状。
- 在完成任务或相关变更后，按需维护对应文档和索引。
- 尝试新建文档前先检查是否已有相同或重叠内容，避免重复文档。
- 如果发现文档已经过时、与代码不一致或相互冲突，应进行修订、合并或删除。

## 项目地图
- alda-agent：项目源码
- docs：项目文档
- ref：参考项目或文件