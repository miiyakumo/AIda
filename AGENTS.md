# 设计原则

以下原则是项目设计、实现和评审的优先约束。

## 1. 简单，直到不能更简单

选择能够完成当前目标的最简单方案。如果删除一个抽象、线程、队列、协议层或持久化机制，
不会破坏当前用户闭环或已经验证的不变量，就应当删除它。复杂度必须由真实需求或已经观察到
的故障证明，不能由“以后可能需要”证明。

## 2. 只解决已经存在的问题

架构工作必须关联至少一项当前用户需求、可复现故障、严重的明确安全风险、已测量性能瓶颈或重复出现
的开发成本。不提前解决多进程、多客户端、海量项目、协议迁移和复杂崩溃一致性等尚未出现的
问题。

## 3. 纵向切片，小步交付

每个阶段都应产生一个可以实际体验的结果，而不是只完成某个横向基础层。一个切片应尽量在
数天内闭合，并包含真实输入、真实 Provider、真实工具和可观察输出。
