interface:
  display_name: "Simple Agent Workflow"
  short_description: "Coordinate complex work with only useful agents"
  default_prompt: "Use $simple-workflow to decide whether this complex task benefits from delegation, then coordinate only the fewest useful agents."
