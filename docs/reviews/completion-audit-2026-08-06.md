# 首期完成度审计

> 日期：2026-08-06
>
> 审计对象：当前工作树、首期需求、初始实施计划和切片 4 实施计划
>
> 结论：实现、离线证据、真实 Alda 和架构重构已闭合；目标尚不能标记完成，因为真实模型创作与
> 人工试听证据缺失

## 1. 证据规则

- “已证明”只用于当前命令输出、生产路径测试或真实 smoke 直接覆盖的结果。
- 固定模型服务只能证明确定性协议和产品状态转换，不能证明真实模型能力。
- Alda fixture 只能证明工具行为，不能证明生成作品的艺术完整性。
- “未检查”不能因没有发现错误而升级为通过。

## 2. 需求逐项审计

| 需求范围 | 状态 | 权威证据 |
|---|---|---|
| 单 crate、Rust 1.85、edition 2024、unsafe/all/pedantic lint | 已证明 | `Cargo.toml`；完整格式、编译和 Clippy 门禁通过 |
| Linux、Java、Alda、Rust 环境检查 | 已证明（当前主机） | `doctor`：Java/Alda/Rust 通过；安装脚本 `--check` 通过 |
| 安装脚本不静默提权、已安装时幂等、不执行远程脚本 | 已证明 | 四项脚本行为集成测试；检查模式、已安装路径和拒绝安装路径中的 fake `sudo` / `curl` 均未被调用；Alda 缺失时只给出人工安装指引 |
| 全新 Linux 实际安装 | 未检查 | 当前没有一次全新系统安装记录；不影响现有主机运行，但不能声称跨新环境验收 |
| 三项显式模型配置、无硬编码默认模型 | 已证明 | 配置单元测试、源码扫描、`.env.example`；缺项在网络前失败 |
| 生效端点/模型可见且 API key 不显示 | 已证明 | `doctor` / smoke 输出路径与项目 secret scan |
| 生产 SSE 文本、工具调用、结束和截断解析 | 已证明 | fixture 直接进入生产解析器；HTTP 分块响应测试 |
| 请求体使用配置模型 | 已证明 | 本地 HTTP 服务捕获请求体并断言 `example-model` |
| 401/429/400/其他 HTTP/网络错误分类 | 已证明 | 生产 HTTP 路径测试 |
| 首次数据外发提示 | 已证明 | 进程内一次性提示测试；提示发生在发请求前 |
| 用户取消流式请求 | 已证明（机制） | abort 测试证明丢弃 chat future 会关闭活动 HTTP 流；CLI `Ctrl+C` 使用同一取消边界 |
| 真实模型简单响应、工具调用和长输出 | 已证明 | 真实 `smoke`：20 个文本事件、`submit_alda` 工具调用、5,831 字节长参数且未截断 |
| Alda 合法/非法解析、时长、乐器三态 | 已证明 | 离线进程测试与真实 `alda-smoke` |
| 空作品不得成为有效结果 | 已证明 | 声部、事件和非零时长检查及失败测试 |
| Alda 超时、进程组终止、stdout/stderr 上限 | 已证明 | 超时、输出上限和黑盒 `Ctrl+C` 无残留进程测试 |
| MIDI、播放和停止 | 已证明（当前主机） | 真实 `alda-smoke`：75 字节 MIDI，play/stop 成功 |
| 完整曲目/即兴模式、指定/排除乐器请求 | 已证明（确定性实现）；真实生成阻塞 | REPL 现可持久化模式、时长和乐器约束；真实完整曲目与 30 秒即兴均在工具调用前产生 4.2–4.7 MB 响应后超时 |
| 最多三轮修正、逐轮截断状态、显式继续 | 已证明 | Agent 多响应测试、REPL `/continue` |
| 范围不清时澄清而非伪造修改 | 已证明 | `needs_input` 生产状态及上下文继续测试 |
| 失败不覆盖当前有效作品 | 已证明 | Project/Agent 单元与项目集成测试 |
| 项目目录、素材、要求、解读、检查、会话保存 | 已证明 | `project.json` 序列化和重启集成测试 |
| 版本 1/2、历史、恢复、恢复后继续不覆盖 | 已证明 | 历史最大版本号加一；恢复后创建版本 3 测试 |
| 人工编辑显式重新采用 | 已证明 | REPL `/reload`；失败恢复当前有效版本 |
| `/play`、`/stop`、`/export`、`/history`、`/restore`、`/quit` | 已证明（命令与确定性行为） | REPL 解析/项目测试；底层真实 Alda 已单独通过 |
| 路径逃逸和损坏元数据负例 | 已证明 | 安全项目名、固定内部路径、版本文件和 JSON 一致性测试 |
| API key 不进入项目、日志或导出 | 已证明 | 类型边界、Git ignore、项目文件扫描；真实 key 不被测试读取或输出 |
| 《机械硬盘之诗》版本化完整闭环 | 已证明（离线） | `e2e_workflow_test.rs`：版本 1→修改→版本 2→恢复/重选→双格式导出→重启 |
| 《机械硬盘之诗》真实模型创作 | 已尝试但阻塞 | 真实项目约束已持久化；当前配置模型连续在工具调用完成前产生数 MB 响应并超时，版本仍为 0 |
| 完整曲目从头到尾人工试听及主观修改效果 | 未检查 | 必须由用户试听，不能用测试替代 |

## 3. 计划产物审计

| 产物 | 状态 |
|---|---|
| `src/project.rs`、`src/repl.rs`、`Agent::modify` | 已实现 |
| 版本、恢复、损坏元数据、路径、CLI、REPL、重启测试 | 已实现并通过 |
| 生产 SSE fixture、HTTP 错误与取消测试 | 已实现并通过 |
| Alda 离线进程、超时、输出上限和取消测试 | 已实现并通过 |
| 完整离线 E2E | 已实现并通过 |
| 架构评审 | 已完成，见 `architecture-review-2026-08-06.md` |
| 评审后重构 | 已完成：取消边界、项目聚合不变量、重复 HTTP test support、错误传播 |
| 分步 Git 提交 | 未执行；当前仍是一组未提交工作树改动 |
| 真实模型与人工验收记录 | 待外部配置和用户试听 |

## 4. 当前机械门禁

以下命令在当前工作树通过：

```bash
cargo fmt --all -- --check
cargo check --all-targets --all-features
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-targets --all-features
bash -n scripts/install-linux.sh
bash scripts/install-linux.sh --check
cargo run -- alda-smoke
```

## 5. 完成目标所需的最小外部动作

1. 用户明确选择一个能够及时完成工具调用的模型 ID，不能由程序猜测。
2. 运行完整曲目与即兴片段真实生成，并覆盖指定/排除乐器。
3. 在真实项目中执行反馈、版本 2、恢复、导出和重启。
4. 用户从头到尾试听并记录主观结果。

在这些证据补齐前，目标仍未完成，并因当前配置模型无法完成创作工具调用保持外部阻塞。
