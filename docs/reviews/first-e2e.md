# 《机械硬盘之诗》首期验收记录

> 日期：2026-08-06
>
> 状态：确定性闭环、真实 Alda 与真实模型连通 smoke 已通过；当前配置模型无法完成创作工具调用，人工试听仍未检查

## 自动化闭环

`tests/e2e_workflow_test.rs` 使用真实生产模块、固定模型服务和固定 Alda 进程，完成：

1. 读取《机械硬盘之诗》fixture；
2. 展示解读和配器理由事件；
3. 生成并校验版本 1；
4. 使用“让中段更冰冷、更机械，结尾的加速和明亮感更强”修改；
5. 生成并校验版本 2；
6. 恢复版本 1，再选择版本 2；
7. 导出 `.alda` 与 MIDI；
8. 退出后重新读取项目，恢复素材、历史和当前版本；
9. 扫描项目元数据，确认测试 API key 未落盘。

结果：通过。

## 真实 Alda smoke

命令：`cargo run -- alda-smoke`

| 能力 | 结果 |
|---|---|
| 合法/非法语法 | 通过 |
| 时长推导 | 通过 |
| 乐器列表 | 通过，129 种 |
| 三态检查 | 通过 |
| MIDI 导出 | 通过，生成 75 字节 fixture MIDI |
| 播放与停止 | 通过 |

## 真实模型证据

`cargo run -- smoke` 已使用显式配置的真实端点和模型通过：

- 普通流式文本：20 个文本事件并正常结束；
- `submit_alda` 工具调用：正常结束并返回参数；
- 长工具输出：5,831 字节，未截断。

随后分别尝试约 180 秒的完整曲目和 30 秒短即兴，均在工具调用完成前持续返回约 4.2–4.7 MB
响应数据，最终由远端/传输超时终止。缩短作品、要求直接调用工具以及限制源码长度均未改变结果。
失败请求没有保存有效版本；真实项目保持版本 0。

因此当前剩余外部证据为：

- 换用用户明确选择、能够及时完成工具调用的模型后，生成完整曲目与即兴片段；
- 证明指定钢琴、排除小提琴的真实模型遵循情况；
- 完成版本 1→修改→版本 2→恢复→导出→重启；
- 由用户从头到尾试听，记录局部修改保持度和主观听感。

这些项目不能用固定响应、语法检查或连通 smoke 冒充。
