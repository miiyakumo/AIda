# 《机械硬盘之诗》首期验收记录

> 日期：2026-08-06；2026-08-12 完成真实单作品复验
>
> 状态：确定性闭环、真实 Alda、真实模型单作品闭环均已通过；人工试听仍待用户确认

## 自动化闭环

`tests/e2e_workflow_test.rs` 使用真实生产模块、固定模型服务和固定 Alda 进程，完成：

1. 读取《机械硬盘之诗》fixture；
2. 展示解读和配器理由事件；
3. 生成并校验版本 1；
4. 使用“让中段更冰冷、更机械，结尾的加速和明亮感更强”修改；
5. 生成并校验版本 2；
6. 恢复版本 1，再选择版本 2；
7. 导出 `.alda` 与 MIDI；
8. 退出后重新读取项目，恢复素材、历史和当前版本；
9. 扫描项目元数据，确认测试 API key 未落盘。

结果：通过。

## 真实 Alda smoke

命令：`cargo run -- alda-smoke`

| 能力 | 结果 |
|---|---|
| 合法/非法语法 | 通过 |
| 时长推导 | 通过 |
| 乐器列表 | 通过，129 种 |
| 三态检查 | 通过 |
| MIDI 导出 | 通过，生成 75 字节 fixture MIDI |
| 播放与停止 | 通过 |

## 真实模型证据

`cargo run -- smoke` 已使用显式配置的真实端点和模型通过：

- 普通流式文本：20 个文本事件并正常结束；
- `submit_alda` 工具调用：正常结束并返回参数；
- 长工具输出：5,831 字节，未截断。

2026-08-12 使用当前显式配置的 `deepseek-v4-flash` 重新运行真实单作品流程。早先的数 MB 输出与
120 秒超时不是模型上下文窗口耗尽，而是模型在完成工具调用前输出过多推演；本次模型能够正常完成
多轮 `submit_alda`。

## 真实单作品闭环（2026-08-12）

项目：`mechanical-drive-poem`

1. 输入《机械硬盘之诗》和约 3 分钟完整纯器乐要求；
2. 模型展示素材解读与配器理由，真实 Alda 将升降号、MIDI 通道、别名和时长诊断逐轮反馈；
3. 版本 1 保存成功：9 声部、891 个可播放事件、约 191 秒；
4. `/play` 成功启动，并保持完整播放时长窗口后显式 `/stop`；
5. 输入“让中段更冰冷、更机械，结尾的加速和明亮感更强”；
6. 模型给出修改候选；由于三轮内容重写导致时长振荡，用 `/reload` 对同一候选只做 tempo 比例校准；
7. 版本 2 保存成功：9 声部、759 个可播放事件、180 秒；
8. `/history`、恢复版本 1、重选版本 2、Alda/MIDI 导出、退出与重新进入均成功；
9. 导出的 `version-0002.alda` 与版本 2 哈希一致，MIDI 是有效 format 0 文件（6,264 字节）；
10. 项目文件未包含配置的 API key。

本次运行同时证明旧结论“当前模型无法完成创作工具调用”已经失效。模型的大上下文窗口与本地
16 MiB 流上限、64 KiB 工具参数上限、120 秒请求超时是不同约束，不能混为一谈。

剩余证据只有用户从头到尾试听版本 1 与版本 2后的主观结果；机器检查不能替代听感判断。
